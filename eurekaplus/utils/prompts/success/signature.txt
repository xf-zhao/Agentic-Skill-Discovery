def object_is_lifted(env: RLTaskEnv, obs_name: str, minimal_height: float) -> torch.Tensor:
    """Sparse reward the agent for lifting an object above a great height."""
    obs = env.obs_buf["observations"]
    object_height = obs[obs_name][:, 2] # x, y, z (height)
    is_success = torch.where(object_height > minimal_height, 1.0, 0.0)
    return is_success


@configclass
class SuccessCfg:
    # Success condition for cube_a to be lifted up to sufficient height, here, a 0.3 meter as an example for "lift very high".
    success = RewTerm(
        func=object_is_lifted,
        params={"minimum_height": 0.3, "obs_name": "cube_a_position"}, 
        weight=30.0,
    )